"""Web link validator package — sitemap-based link health checking.

This package crawls sitemaps, checks every link on every page for
broken or degraded URLs, and produces CSV reports with optional
verification passes. It integrates with the chatbot via tool handlers.
"""